spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/issue_service?useSSL=false&serverTimezone=Asia/Seoul
    username: root
    password: 12345678
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: create
    show-sql: true

server:
  port: 9000
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true